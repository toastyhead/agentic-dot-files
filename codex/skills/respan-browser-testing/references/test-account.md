# Respan staging browser test account

Use the credentials stored in the gitignored
`/Users/rizwan_respan/work/e2e/.env` as `E2E_EMAIL` and `E2E_PASSWORD` only to
authenticate the local Respan frontend against `staging-api.respan.ai` when the
existing browser session is signed out.

Never copy the credentials into commentary, final summaries, screenshots, pull
request descriptions, test artifacts, command output, or repository files.
